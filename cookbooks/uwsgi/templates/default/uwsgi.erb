[uwsgi]
	    plugins = python27
	    chdir = /home/web/proj/<%= @apps %>/
	    pythonpath = ..
	    env = DJANGO_SETTINGS_MODULE=<%= @apps %>.settings
	    module = django.core.handlers.wsgi:WSGIHandler()
	    touch-reload = /home/web/proj/<%= @apps %>/touchme 